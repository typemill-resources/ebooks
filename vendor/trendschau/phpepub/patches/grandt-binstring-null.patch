--- a/BinStringStatic.php
+++ b/BinStringStatic.php
@@ -104,6 +104,9 @@ class BinStringStatic {
     /**
      * @link http://php.net/manual/en/function.strlen.php
      */
     public static function _strlen($string) {
+        if ($string === null) {
+            return 0;
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strlen($string);
@@ -118,6 +121,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strpos.php
      */
     public static function _strpos($haystack, $needle, $offset = 0) {
+        if ($haystack === null) {
+            return false;
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strpos($haystack, $needle, $offset);
@@ -132,6 +138,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strrpos.php
      */
     public static function _strrpos($haystack, $needle, $offset = 0) {
+        if ($haystack === null) {
+            return false;
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strrpos($haystack, $needle, $offset);
@@ -146,6 +155,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.stripos.php
      */
     public static function _stripos($haystack, $needle, $offset = 0) {
+        if ($haystack === null) {
+            return false;
+        }
         if (self::getPHPVersionId() >= 50200 && self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_stripos($haystack, $needle, $offset);
@@ -160,6 +172,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strripos.php
      */
     public static function _strripos($haystack, $needle, $offset = 0) {
+        if ($haystack === null) {
+            return false;
+        }
         if (self::getPHPVersionId() >= 50200 && self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strripos($haystack, $needle, $offset);
@@ -174,6 +189,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strstr.php
      */
     public static function _strstr($haystack, $needle, $before_needle = false) {
+        if ($haystack === null) {
+            return false;
+        }
         if (self::getPHPVersionId() >= 50200 && self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strstr($haystack, $needle, $before_needle);
@@ -188,6 +206,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.stristr.php
      */
     public static function _stristr($haystack, $needle, $before_needle = false) {
+        if ($haystack === null) {
+            return false;
+        }
         if (self::getPHPVersionId() >= 50200 && self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_stristr($haystack, $needle, $before_needle);
@@ -202,6 +223,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strrchr.php
      */
     public static function _strrchr($haystack, $needle) {
+        if ($haystack === null) {
+            return false;
+        }
         if (self::getPHPVersionId() >= 50200 && self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strrchr($haystack, $needle);
@@ -216,6 +240,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.substr.php
      */
     public static function _substr($string, $start, $length = null) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 if (func_num_args() == 2) { // Kludgry hack, as PHP substr is lobotomized.
@@ -239,6 +266,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strtolower.php
      */
     public static function _strtolower($string) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strtolower($string);
@@ -252,6 +282,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strtoupper.php
      */
     public static function _strtoupper($string) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strtoupper($string);
@@ -265,6 +298,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.substr_count.php
      */
     public static function _substr_count($haystack, $needle, $offset = 0, $length = null) {
+        if ($haystack === null) {
+            return 0;
+        }
         if (self::getPHPVersionId() >= 50200 && self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 if (func_num_args() == 2) {
@@ -283,6 +319,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strcmp.php
      */
     public static function _strcmp($str1, $str2) {
+        if ($str1 === null || $str2 === null) {
+            return ($str1 === $str2) ? 0 : (($str1 === null) ? -1 : 1);
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strcmp($str1, $str2);
@@ -296,6 +335,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strcasecmp.php
      */
     public static function _strcasecmp($str1, $str2) {
+        if ($str1 === null || $str2 === null) {
+            return ($str1 === $str2) ? 0 : (($str1 === null) ? -1 : 1);
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strcasecmp($str1, $str2);
@@ -309,6 +351,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strncmp.php
      */
     public static function _strncmp($str1, $str2, $length) {
+        if ($str1 === null || $str2 === null) {
+            return ($str1 === $str2) ? 0 : (($str1 === null) ? -1 : 1);
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strncmp($str1, $str2, $length);
@@ -322,6 +367,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strncasecmp.php
      */
     public static function _strncasecmp($str1, $str2, $length) {
+        if ($str1 === null || $str2 === null) {
+            return ($str1 === $str2) ? 0 : (($str1 === null) ? -1 : 1);
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strncasecmp($str1, $str2, $length);
@@ -335,6 +383,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.strrev.php
      */
     public static function _strrev($string) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_strrev($string);
@@ -355,6 +406,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.mail.php
      */
     public static function _mail($to, $subject, $message, $additional_headers = null, $additional_parameters = null) {
+        if ($to === null || $subject === null || $message === null) {
+            return false;
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_mail($to, $subject, $message, $additional_headers, $additional_parameters);
@@ -370,6 +424,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.utf8_encode.php
      */
     public static function _utf8_encode($string) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_utf8_encode($string);
@@ -383,6 +440,9 @@ class BinStringStatic {
      * @ ... there's no utf8_decode in mbstring ;)
      */
     public static function _utf8_decode($string) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_utf8_decode($string);
@@ -396,6 +454,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.trim.php
      */
     public static function _trim($string, $charlist = null) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_trim($string, $charlist);
@@ -409,6 +468,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.ltrim.php
      */
     public static function _ltrim($string, $charlist = null) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_ltrim($string, $charlist);
@@ -422,6 +484,9 @@ class BinStringStatic {
      * @link http://php.net/manual/en/function.rtrim.php
      */
     public static function _rtrim($string, $charlist = null) {
+        if ($string === null) {
+            return '';
+        }
         if (self::isMBStringOverloaded()) {
             if (self::$USE_MB_ORIG) {
                 return mb_orig_rtrim($string, $charlist);
